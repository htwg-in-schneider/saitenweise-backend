info:
  name: getReviews
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8081/api/review
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
