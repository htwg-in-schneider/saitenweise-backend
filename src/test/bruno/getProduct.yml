info:
  name: getProduct
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8081/api/product/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
