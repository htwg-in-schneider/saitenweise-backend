info:
  name: deleteProduct
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8081/api/product/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
