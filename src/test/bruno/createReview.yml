info:
  name: createReview
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8081/api/review
  body:
    type: json
    data: |-
      {
        "stars": 1,
        "text": "ziemlich schlecht",
        "userName": "Meckerer",
        "product": {
          "id": 1
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
