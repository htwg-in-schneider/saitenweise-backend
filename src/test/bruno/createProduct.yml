info:
  name: createProduct
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/product
  body:
    type: json
    data: |-
      {
        "title": "Bratsche Modell WebTech",
        "description": "Spielt am liebsten mit HTML und CSS.",
        "category": "VIOLA",
        "price": 200.0,
        "imageUrl": "https://neshanjo.github.io/saitenweise-images/viola_beginner.jpg"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
