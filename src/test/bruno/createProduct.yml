info:
  name: createProduct
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/product
  body:
    type: json
    data: |-
      {
        "title": "Bratsche",
        "description": "macht bratsch"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
