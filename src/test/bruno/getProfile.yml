info:
  name: getProfile
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8081/api/profile
  auth:
    type: bearer
    token: INSERT HERE

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
