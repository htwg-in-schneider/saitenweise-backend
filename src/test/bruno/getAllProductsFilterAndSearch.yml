info:
  name: getAllProductsFilterAndSearch
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8081/api/product?name=Modell&category=DOUBLE_BASS
  auth: inherit

params:
  query:
    name: Modell
    category: DOUBLE_BASS

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
