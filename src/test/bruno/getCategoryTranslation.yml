info:
  name: getCategoryTranslation
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8081/api/category/translation
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
