info:
  name: getReviewsByProduct
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8081/api/review/product/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
